```Java
public class Main {
	public static void main(String[] args) {
		String name = "Goose";
		
		// boolean result = name.equals("Goose");   // true
		// int result = name.length();              // 5
		// char result = name.charAt(0);            // G 
		// int result = name.indexOf("o");		    // 1
		// boolean result = name.isEmpty();         // false
		// String result = name.toUpperCase();      // GOOSE
		// String result = name.toLowerCase();      // goose
		// String result = name.replace('o', 'e');  // Geese
		System.out.println(result);
	}
}
```